<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h3>Edit your chat : <%= chats._id %></h3>
    <p>This message was sent by <b><%= chats.from %> </b>to <b><%= chats.to %></b></p>
    <form method="post" action="/chats/<%= chats._id %>?_method=PUT">
        <textarea name="msg" cols="38" rows="6" ><%= chats.msg %></textarea><br>
        <button>Edit</button>
    </form>
</body>
</html>